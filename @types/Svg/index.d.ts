/// <reference path="../string/index.d.ts" />
declare interface Svg {
/** 
* ```
(get) name: string
(set) (Not Available)
```

Specifies the svg name. This is the unique identifier of the svg.

**Note**: the svg name is specified in AR Studio UI during design time.

**See Also**: `SvgsModule.get`.
*/ 
name: string;
} 

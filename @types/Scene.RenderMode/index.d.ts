/** 
 * The RenderMode enum describes how to render a scene object.
 * @property SCREEN_SPACE Indicates that the element should be rendered into the 2D screen space.
 * @property WORLD_SPACE Indicates that the element should be rendered into the 3D world space.
 */ 
declare interface RenderMode{ 
SCREEN_SPACE, 
WORLD_SPACE, 
} 
